{
  "constraints": [
    "TIMING IS THE DOMINANT CONSTRAINT: 2027 is a hard break, not an increment. WPILib 2027 (docs.wpilib.org/en/2027/docs/yearly-overview/yearly-changelog.html) renames Java packages `edu.wpi.first` -> `org.wpilib` and C++ `frc::` -> `wpi::`, requires Java 25 / C++23, upgrades GradleRIO to Gradle 9.4.1 with ZGC default, and targets SystemCore (roboRIO gone). A PumpkinLib written against 2026 APIs will not compile in 2027. Decide NOW whether PumpkinLib targets 2026 (roboRIO, one-season shelf life) or 2027 (SystemCore, org.wpilib). 2027 kickoff is Jan 9, 2027; game is BIOCORE (firstinspires.org/programs/frc/game-and-season).",
    "FIELD COORDINATE ORIGIN MOVES IN 2027. Peter Johnson (WPILib lead) on Chief Delphi: \"Math: field pose origin/orientation – (0,0) is center of field, scoring table at top (180 degree rotation from today)\" (chiefdelphi.com/t/programming-library-changes-for-2027/519900). Any PumpkinLib helper that hardcodes alliance flipping, field dimensions, or blue-origin assumptions must be written against an abstraction, not constants. Flipping becomes a simple negation under center-origin.",
    "DO NOT REIMPLEMENT: swerve kinematics/odometry (WPILib + CTRE SwerveDrivetrain + YAGSL), trajectory generation (PathPlannerLib 2026.1.2, Choreo 2026.0.3), vision pose estimation (PhotonLib 2026.3.4, LimelightHelpers MegaTag2), motor control primitives (Phoenix 6 26.3.0, REVLib 2026.0.5), log replay (AdvantageKit v26.0.2), log visualization (AdvantageScope v26.0.2, bundled in WPILib installer), driver dashboard (Elastic 2026.1.2), or system identification (WPILib SysIdRoutine). Every one of these is mature and free. A new library that competes here will lose.",
    "NETWORKTABLES 4 ONLY from 2027; NT3 is fully removed (docs.wpilib.org/en/2027/docs/yearly-overview/removed-features.html). Shuffleboard and SmartDashboard are removed. Any dashboard integration must target NT4 struct/protobuf publishing consumed by Elastic and AdvantageScope.",
    "HARDWARE APIS REMOVED IN 2027: Relay, AnalogOutput, AnalogGyro, SPI (and all SPI IMUs — ADIS16448, ADIS16470, ADXL345, ADXRS450), DigitalGlitchFilter, AnalogTrigger, DMA, Counter, Ultrasonic, Interrupts, Servo. `XboxController`/`PS4Controller`/`PS5Controller`/`StadiaController` collapse into one `Gamepad` class. `robotInit()` removed (use constructor). `MotorController::set()` -> `setThrottle()`, `StopMotor()` -> `Disable()`. `RamseteCommand`/`RamseteController` removed (use LTV Unicycle). `PIDCommand`/`ProfiledPIDCommand`/`TrapezoidProfileCommand` removed. `MathUtil.clamp()` -> `Math.clamp()`. Rotation2d `value` field removed.",
    "MUTABLE UNITS ARE REMOVED IN 2027 (\"Mutable Java units: Replaced by immutable units\", removed-features page). Do not build a PumpkinLib API around `MutableMeasure`/`MutDistance` for GC avoidance — that escape hatch disappears. Design for immutable `Measure` (Distance, LinearVelocity, Angle, etc.) and accept the allocation, or use raw doubles internally at the hot path.",
    "COMMANDS V2 SURVIVES INTO 2027 alongside v3. Peter Johnson: \"Commands v3 (but v2 is still available)\". Commands v3 is Java-only (C++ has no production coroutines; robotpy will differ). PumpkinLib should target v2 for portability and treat v3 as an optional adapter, OR commit to v3 and accept Java-only. Do not require v3 — it is unfinished and controversial (see the coroutine.yield() footgun discussion).",
    "DISTRIBUTION MUST BE A VENDORDEP JSON + MAVEN, not a copy-paste template. Precedent URLs: PathPlannerLib https://3015rangerrobotics.github.io/pathplannerlib/PathplannerLib.json ; ChoreoLib https://choreo.autos/lib/ChoreoLib2026.json ; DogLog https://doglog.dev/vendordep.json ; FrcCatalyst https://tomas-1226.github.io/FrcCatalyst/vendordep/FrcCatalyst.json ; Studica https://dev.studica.com/maven/release/2026/json/Studica-2026.0.0.json ; ReduxLib https://frcsdk.reduxrobotics.com/ReduxLib_2026.json. AdvantageKit's zip-template distribution is a documented usability failure (see painPoints).",
    "LICENSE COMPATIBILITY: AdvantageKit is BSD-3-Clause-style but headed \"Copyright (c) 2021-2026 Littleton Robotics. All rights reserved.\" with a non-endorsement clause (raw.githubusercontent.com/Mechanical-Advantage/AdvantageKit/main/LICENSE). YAMS is LGPLv3 per its GitHub page — LGPL in a vendordep is a real consideration for teams. FrcCatalyst is MIT. Pick MIT/BSD for PumpkinLib and avoid hard-linking LGPL code into the distributed jar.",
    "MUST WORK OFFLINE AT COMPETITION. No pit internet is assumed. Anything requiring a web service, code generation server, or dependency resolution at deploy time is disqualified. Choreo's Java constants codegen and Epilogue's annotation processor both run at compile time on the laptop — that is the acceptable pattern.",
    "MUST NOT REQUIRE AdvantageKit. AdvantageKit imposes LoggedRobot + IO-layer discipline as an all-or-nothing architecture and its 2027 status is unclear (docs state \"template projects are not currently available for the 2027 alpha versions\"; v27.0.0-alpha-4 is restricted to SystemCore alpha teams). PumpkinLib should be usable with plain TimedRobot + Epilogue/DogLog, and *compatible* with AdvantageKit if the team wants it (YAMS explicitly does this: \"It just requires the boundary to be at setpoints rather than voltage\").",
    "VENDOR NEUTRALITY IS A REAL TRADEOFF, NOT A FREE WIN. Chief Delphi consensus in the FrcCatalyst thread: CTRE-only libraries get deeper features (Motion Magic, FOC, hoot logging, fused CANcoder); vendor-agnostic libraries (YAMS/YAGSL) \"lose a lot of the ctre feature\" but serve more teams. Whichever PumpkinLib chooses, provide YAMS' escape hatch: allow passing a raw vendor config object through, and do not block use of the vendor's own closed-loop control.",
    "SMALL-TEAM SUCCESSION RISK IS A DESIGN CONSTRAINT, NOT A FOOTNOTE. nstrike (YASS) on Chief Delphi re: BLine: \"Projects like MapleSim are waning in support as the developer has graduated and if you don't have a succession plan this may be more of a hindrance for teams that would like to use it for many seasons.\" PumpkinLib needs a documented maintenance/succession plan and a hard rule that a team can rip it out mid-season without rewriting the robot.",
    "MUST DEGRADE TO PLAIN WPILIB. A small team hitting a bug at 11pm before a competition must be able to bypass PumpkinLib for one subsystem without unwinding the whole codebase. Design as composable helpers over a monolithic framework."
  ],
  "painPoints": [
    {
      "problem": "Kickoff-week vendor library lag blocks small teams from starting at all. Templates that depend on multiple vendordeps cannot ship until every vendor publishes, and that can be weeks.",
      "evidence": "AdvantageKit 2026 kickoff thread: jonahb55 said the Spark swerve template \"[is] not yet released since they depend on libraries from vendors who have not published 2026 versions\" and, asked about the TalonFX template, \"Right now we're waiting on PathPlanner to release a 2026 build (ideally also Studica...). I'm not sure what PathPlanner's timeline is.\" NavX IO implementations were shipped disabled until Studica published. https://www.chiefdelphi.com/t/advantagekit-2026-replay-refined/509227",
      "libraryFix": "PumpkinLib should have ZERO hard vendordep dependencies in its core artifact. Use reflection-free optional integration: compile against interfaces, detect vendor classes at runtime (Class.forName / ServiceLoader), and ship vendor adapters as separate optional artifacts. A team should be able to install PumpkinLib on kickoff day with only WPILib present and add Phoenix/REV/PathPlanner adapters as they land."
    },
    {
      "problem": "AdvantageKit's zip-template distribution model is a documented onboarding failure for exactly the teams a small-team library targets.",
      "evidence": "AfterTen on Chief Delphi: \"I don't get how the template projects work... When I look in the repository I see 'template_projects' and inside that then 'sources', 'template'... If I use the 'template' folder and treat it as the root of a robot project... I get an error that a team number cannot be set because it is not a WPILib project.\" rickmaster: \"When trying to build the TalonFX template, I get an error: 'Cannot Build Robot Code since this is not a WPILib project'. All I did was unzip the project and attempt to build.\" AdvantageKit docs also say \"Manually updating projects to 2026 is not recommended due to the risk of subtle breaking changes.\" https://www.chiefdelphi.com/t/advantagekit-2026-replay-refined/509227",
      "libraryFix": "Distribute as a vendordep, not a template. A team's existing project must keep working after `Install` with zero file moves. If PumpkinLib needs scaffolding, generate it with a WPILib VS Code example project or an annotation processor — never a zip the student has to unpack into the right place."
    },
    {
      "problem": "Every team hand-rolls the same tunable-constant plumbing. There are at least six independent, incompatible implementations in the community and none of them is in WPILib for 2026.",
      "evidence": "Chief Delphi \"How are team's implementing adjustable constants\" (https://www.chiefdelphi.com/t/how-are-teams-implementing-adjustable-constants/507361) surfaced, in one 16-post thread: WPILib Sendables, 6328's `LoggedTunableNumber` (github.com/Mechanical-Advantage/RobotCode2025Public .../util/LoggedTunableNumber.java), DogLog's `DogLog.tunable(...)`, YAMS Live Tuning (yagsl.gitbook.io/yams), Team 340's custom `/Tunables` NT table (bryceroethel), pjreiniger's Preferences-wrapper vendordep, and CTRE Tuner X device configs. bryceroethel: \"there's currently a PR targeting the 2027 release of WPILib that includes a Tunables API\" — i.e. still not shipped as of 2026.",
      "libraryFix": "A first-class Tunable primitive is the single highest-value, lowest-risk thing PumpkinLib can ship. Requirements from the thread: (1) compiles to a constant when tuning mode is off (6328's pattern), so zero competition overhead; (2) persists to disk on the controller so values survive redeploy/power-cycle (pjreiniger's ask); (3) publishes under a single NT root readable by Elastic AND AdvantageScope tuning mode; (4) works for PID gains, feedforward, AND setpoints; (5) supports per-robot values (practice vs comp bot). Design the API so it can be re-pointed at the WPILib 2027 Tunables API when that lands."
    },
    {
      "problem": "SmartDashboard/telemetry causes loop overruns on small-team robots, and the team's instinct is to log LESS — which destroys their ability to debug.",
      "evidence": "brainiac on Chief Delphi: \"Is there a way to do this without using smart dashboard? When we use smart dashboard, our loop times almost always overrun, so we are trying to limit our use of smart dashboard.\" https://www.chiefdelphi.com/t/how-are-teams-implementing-adjustable-constants/507361 . Epilogue's own docs warn that overhead \"comes from calling expensive methods (especially repeated CAN bus queries)\" and that `Epilogue.bind()` runs at a half-phase offset from the 50Hz loop, desynchronizing logged values from control values.",
      "libraryFix": "Ship a telemetry policy layer, not just telemetry: per-signal rate limiting, a competition/debug importance switch (Epilogue has `minimumImportance` but teams don't wire it), CAN signal caching so the same `getPosition()` is read once per loop and reused by both control and logging, and a built-in loop-time budget alert that names the offending subsystem. This is a genuine gap — nobody ships it."
    },
    {
      "problem": "Epilogue's annotation-based logging has hard structural limits that force teams back to manual logging exactly where it hurts.",
      "evidence": "WPILib docs (docs.wpilib.org/en/stable/docs/software/telemetry/robot-telemetry-with-annotations.html): cannot log static members, private methods, or methods taking arguments; null values throw NPE into the error handler because \"log data cannot represent nulls\"; \"Logged fields use their declared type, not runtime subtypes\" — so an interface-typed field (i.e. every IO-layer field) logs via the interface, not the concrete impl; vendor types need a hand-written `@CustomLoggerFor` `ClassSpecificLogger`. 2026 improved this somewhat (superclass field/method logging via reflection, protobuf support, backend time/memory optimization) but the core limits remain.",
      "libraryFix": "Ship pre-built `@CustomLoggerFor` loggers for TalonFX/TalonFXS/SparkMax/SparkFlex/CANcoder/CANrange/Pigeon2/PhotonCamera so a small team gets rich per-device telemetry for free. This is pure grunt work no small team will ever do, it is exactly the \"depend, don't reimplement\" sweet spot, and it composes with Epilogue rather than replacing it."
    },
    {
      "problem": "Swerve bring-up remains the single biggest small-team failure mode, and the failure is almost never in the math — it is in configuration, IDs, offsets, and inversions.",
      "evidence": "Live example: \"URGENT HELP- Swerve Code (AdvantageKit Spark Template)\" — EceSoylu: \"it is our first year in FRC as programmers. We can't really figure it out trough the documents... We have to make the robot move and turn perfectly... so that the team won't die this year.\" The community's diagnostic reply (BobbyVanNess) is a pure checklist of *configuration* faults: \"check your defined x and y positions of each swerve module, check all of your devices in phoenix tuner... blink them to identify one by one and verify they match how they're defined in your code. Make sure they're up to date on firmware and don't ignore any issues or duplicate ID's. Set your wheels straight (bevels facing the same way) and verify your offsets are all correct. Wiggle one wheel at a time and verify that the encoder value you expect to see change changes.\" https://www.chiefdelphi.com/t/urgent-help-swerve-code-advantagekit-spark-template/515511",
      "libraryFix": "Encode BobbyVanNess's checklist as EXECUTABLE CODE. A `PumpkinLib.verify()` / bring-up wizard command that at robot init: enumerates configured CAN IDs, reports duplicates and missing devices as Alerts, checks firmware versions, and offers an interactive per-module routine (drive one module, confirm which wheel moved; spin one wheel, confirm which encoder changed; auto-capture steer offsets with wheels aligned). Nobody ships this. It directly addresses the documented #1 failure. This is the strongest differentiating feature identified in this research."
    },
    {
      "problem": "There is no standard pre-match self-test / health check, so small teams discover a dead motor or unplugged CANcoder on the field.",
      "evidence": "FrcCatalyst advertises \"health check-based fault monitoring\" and \"temperature cutoff, limit-switch auto-zero\" as headline differentiators (tomas-1226.github.io/FrcCatalyst), which indicates the need is real and unmet in WPILib. WPILib's Alert API exists (`Alert`, `AlertType` kError/kWarning/kInfo, published to `/SmartDashboard/<Group>`, docs.wpilib.org/en/stable/docs/software/telemetry/persistent-alerts.html) but WPILib ships zero built-in alerts — the team must author every condition themselves.",
      "libraryFix": "Ship a library of pre-authored Alerts that light up automatically: CAN device not present, CAN utilization high, firmware mismatch, motor over-temperature, encoder not moving while commanded, battery sag under threshold, loop overrun, vision camera disconnected, gyro not calibrated. Plus a `SystemCheckCommand` that exercises each mechanism through its range and reports pass/fail. Both build on stock WPILib Alert — no reinvention, huge value."
    },
    {
      "problem": "WPILib's units library has aliasing and allocation footguns, and the workaround (MutableMeasure) is being deleted in 2027.",
      "evidence": "WPILib docs (docs.wpilib.org/en/stable/docs/software/basic-programming/java-units.html) warn: \"if we run the `calculateDelta` method a few times\" with mutable inputs, deltas become zero because \"the `m_lastDistance` field is a reference to the *same* `MutDistance` object as the input\"; \"Default operations allocate new objects\"; `PerUnit`/`MultUnit` need `ofNative()` not `of()`. Meanwhile the 2027 removed-features page lists \"Mutable Java units: Replaced by immutable units\".",
      "libraryFix": "Take a position and enforce it: PumpkinLib's public API uses immutable `Measure` types (Distance, Angle, LinearVelocity, Voltage) at configuration boundaries only, and raw doubles in per-loop hot paths. Document the boundary explicitly. Do not expose MutableMeasure anywhere — it is a 2027 landmine."
    },
    {
      "problem": "Commands v3's coroutine model introduces a silent, uncheckable footgun that will disproportionately hurt small teams.",
      "evidence": "catrix on Chief Delphi: \"if I somehow forget to add this single line [coroutine.yield()], MY ROBOT WILL EXPLODE. And I get absolutely zero warning or error about it during compile or deploy... It's also very hard to explain coroutine.fork() and coroutine.yield() to a junior programmer.\" SamCarlberg (WPILib) conceded: \"Yep. That's the glaring footgun with the coroutine-first approach; there's no way to check at compile time so it's up to the programmer to be aware of it. A watchdog will be necessary...\" https://www.chiefdelphi.com/t/wpilib-commands-v3-imperative-function-bodies-with-coroutines/500699",
      "libraryFix": "Do not build PumpkinLib's core on Commands v3. Build on v2 (which Peter Johnson confirms remains available in 2027), and if v3 support is added, ship the missing watchdog: a scheduler wrapper that detects a command exceeding its time budget without yielding and raises a named Alert identifying the command. Oblarg's framing is the safe one: treat v3 as \"a more expressive alternative to the current ProxyCommand\", not the default."
    },
    {
      "problem": "Sim-vs-real divergence: teams write two code paths and the sim path rots, so pre-season/offseason work doesn't transfer.",
      "evidence": "YASS markets its core value as \"your team to rapidly prototype and develop seamlessly in simulation and the robot with the same code\" (yetanothersoftwaresuite.com), and YAMS exposes `simIterate()` with physics models built in. maple-sim exists precisely because WPILib sim can't model game-piece interaction. nstrike on the value: \"The best part is you don't have to have a robot with you to test this. Its a huge timesaver!\" — which is the defining constraint of a small team with limited robot access.",
      "libraryFix": "Make simulation a non-optional, zero-config property of every PumpkinLib mechanism: if you declare gearing, mass, and moment of inertia in the config, the WPILib physics model (ElevatorSim/SingleJointedArmSim/FlywheelSim/DCMotorSim) is wired automatically with no second code path. FrcCatalyst and YAMS both do this and both cite it as their top selling point. Optionally bridge to maple-sim for game-piece physics — but only optionally (see maintenance risk below)."
    },
    {
      "problem": "Community libraries carry succession risk that small teams cannot absorb, and maple-sim is the cautionary example.",
      "evidence": "nstrike (YASS maintainer) on Chief Delphi: \"Projects like MapleSim are waning in support as the developer has graduated and if you don't have a succession plan this may be more of a hindrance for teams that would like to use it for many seasons.\" maple-sim's own 2026 release is labeled \"This is a beta version of the library, bugs and glitches may exist\" with latest v0.4.0-beta (github.com/Shenzhen-Robotics-Alliance/maple-sim/releases). BLine is explicitly \"built by Team 2638 Rebel Robotics, by students for students\" with development started June 2025.",
      "libraryFix": "Treat this as a marketing and architecture requirement: publish a succession plan, keep the dependency graph shallow (WPILib + optional vendor adapters only), and never make a beta third-party library (maple-sim) a required dependency. Version PumpkinLib by season (2027.x.y) so teams can pin."
    },
    {
      "problem": "Multi-robot constants (practice bot vs competition bot) is a universal need with no standard solution.",
      "evidence": "3061-lib's README calls it out as a headline feature: \"supports multiple robots with different configurations, which is useful for supporting multiple physical robots with the same code base (e.g., a practice bot and a competition bot)\" (github.com/HuskieRobotics/3061-lib). On CD, githubAnish recommends a base-constants-class-plus-subclass pattern and pjreiniger's vendordep persists Preferences to disk \"Also very useful if you have two robots that need different constants.\"",
      "libraryFix": "Ship robot-identity resolution (roboRIO/SystemCore serial number or a jumper/DIO strap) plus a constants-layering mechanism, integrated with the Tunable system so a value tuned on the practice bot can be promoted to the comp bot's persisted store. Small lift, universally needed, nobody standardizes it."
    },
    {
      "problem": "PathPlannerLib deliberately froze in 2026, leaving a one-season window where autonomous tooling is stable but stagnant — and it will change for SystemCore.",
      "evidence": "mjansen (PathPlanner author, username kreeve) announcing the 2026 release: \"There have been no major changes of note this year besides the addition of the 2026 field image. My plan is to hold off on major changes for the 2027 switch to SystemCore.\" https://www.chiefdelphi.com/t/advantagekit-2026-replay-refined/509227 . Latest is v2026.1.2 (Jan 12, 2026), whose only change was fixing the 2026 field image scale.",
      "libraryFix": "Do not wrap PathPlannerLib's API surface tightly — it will move for 2027. Wrap only the four things AutoBuilder actually needs from a drivetrain (pose supplier, pose reset, robot-relative ChassisSpeeds supplier, ChassisSpeeds consumer) behind a PumpkinLib drivetrain interface, so swapping PathPlanner <-> Choreo <-> BLine is a one-line change for the team."
    },
    {
      "problem": "Teams still hand-write the Choreo/PathPlanner glue and the alliance-flip logic, and get it wrong.",
      "evidence": "Choreo 2026.0.0 added Java constants codegen specifically because \"trajectories were loaded by providing the String name of the trajectory, but there was no compile-time check to ensure the referenced trajectories actually existed\" and \"Many teams asked for a way to access the defined pose variables from robot code, and several developed systems to parse the .chor file at runtime\" (github.com/SleipnirGroup/Choreo/releases). PathPlanner AutoBuilder requires the user to supply a red-alliance boolean supplier by hand.",
      "libraryFix": "Choreo already solved the codegen half — depend on it. The remaining gap PumpkinLib can close: a single correct, tested alliance/origin abstraction that works for both 2026 blue-origin and 2027 center-origin conventions, so teams write `Field.flipIfRed(pose)` once and it keeps working across the coordinate change."
    }
  ],
  "patterns": [
    {
      "name": "IO-layer hardware abstraction (AdvantageKit pattern)",
      "whatItDoes": "Splits each subsystem into a plain-data `Inputs` struct, an `IO` interface with `updateInputs(inputs)` plus setter methods, and vendor-specific `IOTalonFX`/`IOSpark`/`IOSim` implementations. The subsystem logic reads only from the inputs struct, so the entire robot state can be deterministically replayed from a log in simulation.",
      "whereSeen": "https://docs.advantagekit.org/ ; templates at https://github.com/Mechanical-Advantage/AdvantageKit/releases (v26.0.2, 2026-03-19). Adopted by 3061-lib (https://github.com/HuskieRobotics/3061-lib), FRC 1678 C2026-Public, FRC 2910. AdvantageKit reports 600+ teams in 2025, 200+ using its swerve templates (https://www.chiefdelphi.com/t/advantagekit-2026-replay-refined/509227).",
      "libraryValue": "This is the single most-copied architecture in FRC and PumpkinLib should be shaped so it composes with it rather than fighting it. AdvantageKit already owns replay; do not rebuild it. The gap: the IO-layer split costs a small team ~3x the boilerplate per subsystem and there is no code generation for it. An annotation processor that generates the `Inputs` class and the `@AutoLog` plumbing from a single annotated interface, and that degrades to direct calls when AdvantageKit is absent, is high-value and non-duplicative.",
      "codeSketch": "// current cost per subsystem: 3 files, ~120 lines\n@AutoLog class ElevatorIOInputs { double positionRad; double velocityRadPerSec; double appliedVolts; double currentAmps; }\ninterface ElevatorIO { default void updateInputs(ElevatorIOInputs i) {} default void setVoltage(double v) {} }\nclass ElevatorIOTalonFX implements ElevatorIO { ... }  class ElevatorIOSim implements ElevatorIO { ... }\n// PumpkinLib target: one declaration, generated IO + sim + logging"
    },
    {
      "name": "Declarative mechanism config builder (YAMS / FrcCatalyst pattern)",
      "whatItDoes": "A single builder describes a mechanism's physical reality (motor type, CAN IDs, gear ratio, drum radius/length, mass/MOI, soft limits, current limits) and its control (PID slots, feedforward incl. gravity term, Motion Magic or ProfiledPID constraints, named positions). The library derives closed-loop control, unit conversion, soft/hard limits, a WPILib physics sim model, telemetry, and SysId routines from that one declaration.",
      "whereSeen": "YAMS: https://github.com/Yet-Another-Software-Suite/YAMS , https://yagsl.gitbook.io/yams (mechanisms: Arm, Elevator, Pivot/turret/hood, Flywheel, SwerveDrive; LGPLv3). FrcCatalyst v1.8.0: https://tomas-1226.github.io/FrcCatalyst/ (LinearMechanism, RotationalMechanism, FlywheelMechanism, RollerMechanism, WinchMechanism, ClawMechanism, DifferentialWristMechanism, PneumaticMechanism, Superstructure; MIT; WPILib 2026.2.1 + Phoenix 6 26.1.1). Claimed reduction: elevator with gravity FF ~150 lines -> ~8 lines.",
      "libraryValue": "This is the proven core value proposition for small teams and the pattern PumpkinLib should adopt. Two independent projects converged on it in 2026 and both are being used in competition, which validates demand. The differentiators still open: (a) FrcCatalyst is CTRE-only, (b) YAMS is LGPLv3 and vendor-agnostic at the cost of vendor-specific features, (c) neither has a strong bring-up/verification story, (d) neither is designed for the 2027 org.wpilib break. PumpkinLib can win on 2027-first design + verification tooling rather than on the builder itself.",
      "codeSketch": "// FrcCatalyst\nLinearMechanism elevator = new LinearMechanism(LinearMechanism.Config.builder()\n    .name(\"Elevator\").motor(13).follower(14, true).motorType(MotorType.KRAKEN_X60)\n    .gearRatio(10.0).drumRadius(0.0254).mass(5.0)\n    .pid(50, 0, 0.5).gravityGain(0.35).motionMagic(2.0, 4.0, 20.0)\n    .position(\"STOW\", 0.0).position(\"HIGH\", 1.1).build());\n// -> free: soft limits, current limits, limit-switch auto-zero, temp cutoff, sim, NT telemetry, SysId"
    },
    {
      "name": "Vendor-normalizing SmartMotorController with a config escape hatch",
      "whatItDoes": "One `SmartMotorController` interface over Phoenix 6 TalonFX/TalonFXS, REVLib SparkMax/SparkFlex, and ThriftyNova, normalizing configuration, closed-loop setpoints, feedforward, motion profiles, and telemetry. Crucially it does NOT hide the vendor: teams can pass a raw vendor config object through, and can keep using the vendor's own onboard closed-loop control.",
      "whereSeen": "YAMS `yams.motorcontrollers.SmartMotorController` / `SmartMotorControllerConfig` / `yams.motorcontrollers.remote.TalonFXSWrapper`. Docs: https://yagsl.gitbook.io/yams (Passing a Vendor Config; CTRE-Specific Notes). nstrike on CD: \"We have an escape hatch for every vendor allowing users to pass in whatever vendor config thats applicable. We also don't prevent the user from using the vendors SMC if they want to.\"",
      "libraryValue": "The escape-hatch design is the key lesson and PumpkinLib should copy it verbatim as a rule. Community criticism of over-abstraction is specific and worth heeding — tcrvo on CD: \"This encourages overabstraction if the shared namespace becomes anything more than a way to keep the import list shorter... subtle differences pile up.\" Also note the honest limitation nstrike names: setpoint caching breaks when the user drives the vendor SMC directly, \"most SMC's dont expose the previous setpoint.\" Depend on YAMS or copy its interface shape; do not build a leakier version."
    },
    {
      "name": "Tunable value bound to NetworkTables with a compile-out switch",
      "whatItDoes": "A double/boolean that reads from NT under a well-known root when a `tuningMode` flag is set, and returns a hardcoded default otherwise. Some variants persist to disk on the controller so values survive redeploy and power cycle.",
      "whereSeen": "6328 `LoggedTunableNumber` (github.com/Mechanical-Advantage/RobotCode2025Public, src/main/java/org/littletonrobotics/frc2025/util/LoggedTunableNumber.java) — gated on `Constants.tuningMode`. DogLog `DogLog.tunable(\"Intake/Voltage\", 4.5)` returning a `DoubleSubscriber` (https://doglog.dev/). YAMS Live Tuning exposed as a Command writing to `NT:/Tuning`. Team 340's `/Tunables` table. pjreiniger's Preferences-wrapper vendordep (persists to disk). WPILib Tunables API is a PR targeting 2027, not shipped. Full thread: https://www.chiefdelphi.com/t/how-are-teams-implementing-adjustable-constants/507361",
      "libraryValue": "HIGHEST-VALUE GAP. Six incompatible community implementations, zero in WPILib for 2026, and the WPILib 2027 API is still a PR. A well-designed PumpkinLib Tunable — compile-out for competition, disk-persisted, per-robot, working in Elastic and AdvantageScope tuning mode, covering gains AND setpoints — would be immediately adopted and is squarely in the 'accelerate in-season tuning' mission.",
      "codeSketch": "// target API\nTunable kP = Tunable.of(\"Elevator/kP\", 50.0);   // persisted, NT-backed in tuning mode, constant otherwise\nTunable.onChange(kP, elevator::setP);            // push-based, no polling in the hot loop"
    },
    {
      "name": "Annotation-driven telemetry with compile-time codegen (Epilogue)",
      "whatItDoes": "`@Logged` on a class causes a WPILib annotation processor to generate logging code at compile time (into build/generated/sources/annotationProcessor). Supports opt-out (`@NotLogged`) or opt-in modes, `importance` filtering (DEBUG/INFO/CRITICAL), `loggingPeriod`, pluggable backends (NetworkTables default, `FileBackend(DataLogManager.getLog())` for disk-only), and `@CustomLoggerFor` + `ClassSpecificLogger` for third-party types. 2026 added superclass field/method logging, reflection access to non-public superclass fields, protobuf-serializable type support, and backend time/memory optimization.",
      "whereSeen": "https://docs.wpilib.org/en/stable/docs/software/telemetry/robot-telemetry-with-annotations.html ; 2026 changelog https://docs.wpilib.org/en/stable/docs/yearly-overview/yearly-changelog.html",
      "libraryValue": "Epilogue is in-box, free, zero-runtime-reflection, and already integrated with Phoenix 6 (CTRE 2026 added an Epilogue Backend for Signal Logger). PumpkinLib must NOT ship a competing logging API. The productive contribution is a set of ready-made `@CustomLoggerFor` loggers for every common vendor device — the thing every team needs and none writes."
    },
    {
      "name": "Trigger-based autonomous composition (Choreo AutoFactory / PathPlanner AutoBuilder)",
      "whatItDoes": "Auto routines are expressed as trajectories plus event triggers rather than imperative sequences. Choreo: `AutoFactory.newRoutine()` -> `AutoRoutine.trajectory(name)` -> `AutoTrajectory` with `done()`/`doneDelayed()`/`atTime()` triggers, alliance flipping, FMS reporting. PathPlanner: `AutoBuilder` configured with pose supplier, pose reset, robot-relative ChassisSpeeds supplier, ChassisSpeeds consumer, controller, `RobotConfig.fromGUISettings()`, and a red-flip BooleanSupplier; plus NamedCommands and EventMarkers.",
      "whereSeen": "https://choreo.autos/choreolib/auto-factory/ , https://choreo.autos/api/choreolib/java/choreo/auto/AutoRoutine.html ; https://pathplanner.dev/pplib-getting-started.html , https://pathplanner.dev/pplib-build-an-auto.html . Interop: https://pathplanner.dev/pplib-choreo-interop.html",
      "libraryValue": "Both are mature and free — depend, don't rebuild. The reusable insight for PumpkinLib is the minimal drivetrain contract: pose supplier + pose reset + robot-relative speeds supplier + speeds consumer. Define exactly that interface once and every path library plugs in. That also insulates teams from PathPlanner's announced 2027 rework."
    },
    {
      "name": "Compile-time constants generated from path/field data (Choreo 2026)",
      "whatItDoes": "Choreo 2026 generates Java constants classes `ChoreoTraj` and `ChoreoVars` from the .chor project so trajectory names and pose variables are compile-time checked instead of magic strings. Motivated explicitly by teams writing their own runtime .chor parsers.",
      "whereSeen": "https://github.com/SleipnirGroup/Choreo/releases (v2026.0.0 Jan 11 2026, v2026.0.2 Mar 2 2026 added the Java codegen + alliance-flipped view layer); https://choreo.autos/usage/code-generation/",
      "libraryValue": "Validates compile-time codegen as the right delivery mechanism for FRC (runs on the laptop, no runtime cost, no pit internet). PumpkinLib should reuse this delivery model for its own generated artifacts (IO classes, constants, device registries) rather than runtime reflection or JSON parsing."
    },
    {
      "name": "JSON/declarative full-drivetrain configuration (YAGSL)",
      "whatItDoes": "An entire swerve drivetrain — modules, motor and encoder types, CAN IDs, offsets, gear ratios, physical properties, PID — is declared in JSON files rather than code. Vendor-agnostic across REV, CTRE, Studica, ThriftyNova; includes vision pose fusion and simulation.",
      "whereSeen": "https://github.com/Yet-Another-Software-Suite/YAGSL , https://docs.yagsl.com/ . 2026 releases 2026.1.13 through 2026.8.05 (2026-08-05, whose notes include \"Update to 2027\"). YASS reports 537 FRC teams used its libraries this season (https://yetanothersoftwaresuite.com/).",
      "libraryValue": "YAGSL owns configuration-driven swerve for small teams and is ALREADY moving to 2027 (2026.8.05). PumpkinLib should not build a swerve library. The transferable pattern is data-driven config with validation — and the open gap is that JSON config trades compile-time safety for editability. A typed-builder-plus-optional-JSON hybrid with a validating bring-up check is the differentiated position."
    },
    {
      "name": "WPILib Alert API as a fault-surfacing channel",
      "whatItDoes": "`Alert` objects with `AlertType` (kError/kWarning/kInfo) that are `set(boolean)` each loop with built-in change detection, published to NetworkTables at `/SmartDashboard/<Group Name>` (default group \"Alerts\") and rendered natively by Elastic and AdvantageScope.",
      "whereSeen": "https://docs.wpilib.org/en/stable/docs/software/telemetry/persistent-alerts.html",
      "libraryValue": "The transport exists and dashboards already render it — but WPILib ships no actual alerts. This is the ideal 'depend, don't reimplement' target: PumpkinLib supplies the alert CONTENT (device missing, firmware mismatch, over-temp, encoder stalled, CAN utilization, loop overrun, battery sag, camera disconnected) over WPILib's transport. High perceived value, near-zero architectural risk.",
      "codeSketch": "Alert disconnected = new Alert(\"Elevator\", \"TalonFX 13 not on CAN bus\", AlertType.kError);\ndisconnected.set(!motor.isConnected());"
    },
    {
      "name": "SysIdRoutine characterization built into the mechanism",
      "whatItDoes": "WPILib `SysIdRoutine` runs quasistatic and dynamic voltage sweeps, logs to WPILOG/DataLog, and the desktop SysId tool (bundled in the WPILib installer) fits kS/kV/kA/kG for simple-motor, elevator (gravity), and arm (cosine gravity) models.",
      "whereSeen": "https://docs.wpilib.org/en/stable/docs/software/advanced-controls/system-identification/introduction.html . Integrated as a first-class feature by FrcCatalyst (\"SysId integration\"), YAMS, maple-sim (\"Motor characterization (SysId functions for drive and steer motors)\"), and 3061-lib.",
      "libraryValue": "WPILib provides the routine and the fitting tool; the friction is that a team must hand-write the routine, the log-motor callbacks, and the button bindings for every mechanism, then manually retrieve the file. PumpkinLib should auto-generate the SysIdRoutine from the same mechanism declaration used for control and sim, and expose it as a ready-bound command — the 'declare once, get characterization free' payoff."
    },
    {
      "name": "Physics-engine simulation with game-piece interaction (maple-sim)",
      "whatItDoes": "Wraps the dyn4j 2D rigid-body engine to simulate collisions between robots, field elements, and game pieces — beyond WPILib's per-mechanism models. Simulates swerve drivetrains with configurable modules and friction, intakes with game-piece capture conditions, arena/field elements, and battery voltage sag under load.",
      "whereSeen": "https://github.com/Shenzhen-Robotics-Alliance/maple-sim ; releases v0.4.0-beta (2026 Rebuilt, WPILib 2026.2.1), prior stable v0.3.14. Explicitly labeled beta: \"bugs and glitches may exist\", \"Expect occasional instability, bugs, and API changes.\"",
      "libraryValue": "Fills the one simulation gap WPILib genuinely has (game-piece physics), which matters enormously for a small team with limited robot access. But it is beta AND flagged by the community as having maintenance/succession risk (see constraints). Integrate as an OPTIONAL adapter behind an interface, never a required dependency."
    },
    {
      "name": "Swerve setpoint generator (Team 254 lineage)",
      "whatItDoes": "Takes a previous setpoint and a desired ChassisSpeeds and produces an achievable next setpoint that respects module steering-rate limits and wheel velocity/torque limits, preventing skid and module-fight.",
      "whereSeen": "`com.pathplanner.lib.util.swerve.SwerveSetpointGenerator` — https://pathplanner.dev/pplib-swerve-setpoint-generator.html ; docs credit it as \"based on a version created by FRC team 254\". Also present in CTRE's swerve API via `WheelForceCalculator` (Phoenix 6 2026).",
      "libraryValue": "Already free in PathPlannerLib and CTRE. Never reimplement. Worth knowing so PumpkinLib's drivetrain interface passes ChassisSpeeds through a pluggable setpoint filter slot rather than commanding modules directly."
    },
    {
      "name": "Polyline / point-to-point path following as an alternative to optimized trajectories",
      "whatItDoes": "Represents paths as simple polylines with per-segment constraints, trading theoretical time-optimality for fast iteration and cheap computation (claimed 97% compute reduction vs PathPlanner, ~2.6% theoretical time penalty). Two parts: a Python/PySide6 GUI editor and a Java following library.",
      "whereSeen": "BLine by FRC 2638 Rebel Robotics — https://www.chiefdelphi.com/t/introducing-bline-a-new-rapid-polyline-autonomous-path-planning-suite/509778 . Now listed by WPILib as a PathWeaver replacement alongside Choreo and PathPlanner (docs.wpilib.org/en/2027/docs/yearly-overview/removed-features.html).",
      "libraryValue": "The relevant insight for a small-team library is BLine's stated rationale, echoed by nstrike: PathPlanner and Choreo are \"significantly harder to tune and get right, but when it works, it really shines.\" Tunability beats optimality for teams with little practice time. The real durable use case is teleop drive-to-pose / auto-align, not full autos. PumpkinLib should ship a solid drive-to-pose command and let the team pick a trajectory library."
    },
    {
      "name": "Multi-robot constant layering (practice bot vs comp bot)",
      "whatItDoes": "Resolves robot identity at runtime and layers constants so one codebase serves several physical robots with different offsets, gains, and geometry.",
      "whereSeen": "3061-lib README: \"supports multiple robots with different configurations... (e.g., a practice bot and a competition bot)\" — https://github.com/HuskieRobotics/3061-lib (v2026.1.0; SDS MK4/MK4i/MK4n/MK5n + Falcon/Kraken + CANcoder + Pigeon out of the box, HAL for others). Also githubAnish's RobotHardware base-class-plus-subclass pattern and pjreiniger's disk-persisting Preferences vendordep on https://www.chiefdelphi.com/t/how-are-teams-implementing-adjustable-constants/507361",
      "libraryValue": "Small lift, universal need, no standard implementation. Pairs naturally with the Tunable primitive: tune on the practice bot, promote the value into the comp bot's persisted store. Good candidate for an early PumpkinLib win."
    }
  ],
  "source": "Web research conducted 2026-08-06 across ~26 WebSearch/WebFetch calls plus direct Discourse JSON pulls from Chief Delphi (which 403s WebFetch; retrieved via curl + .json endpoints). Primary sources only: docs.wpilib.org (stable/2026 and /2027), github.com/wpilibsuite/allwpilib + frc-docs + SystemcoreTesting, wpilib.org/blog, v6.docs.ctr-electronics.com + api.ctr-electronics.com/changelog, docs.revrobotics.com, docs.advantagekit.org + docs.advantagescope.org + littletonrobotics.org, github.com/Mechanical-Advantage, github.com/mjansen4857/pathplanner + pathplanner.dev, github.com/SleipnirGroup/Choreo + choreo.autos, github.com/PhotonVision/photonvision, docs.limelightvision.io, github.com/Yet-Another-Software-Suite (YAGSL/YAMS) + yetanothersoftwaresuite.com + docs.yagsl.com, github.com/Gold872/elastic_dashboard, github.com/Shenzhen-Robotics-Alliance/maple-sim, github.com/HuskieRobotics/3061-lib, doglog.dev, tomas-1226.github.io/FrcCatalyst, and eight Chief Delphi threads (IDs 500699, 506826, 507361, 509227, 509778, 515511, 519900, 522893). CAVEATS: (1) GitHub release-date extraction for PhotonVision returned 2025 dates for v2026.x releases — the version numbers are reliable, the dates are not; treat PhotonVision dates as unverified. (2) YAMS version numbers and license could not be confirmed from a LICENSE file (raw fetch 404'd); the GitHub page states LGPLv3 — verify before depending. (3) The WPILib 2027 Tunables API is described on Chief Delphi as an open PR, not merged — do not treat as shipped. (4) 2027 details come from alpha documentation (Alpha 5/6) and are subject to change before the Jan 9, 2027 kickoff.",
  "stack": "CURRENT USER STACK (read from local repos, not the web): 0000-XXXX-Robot-Template/vendordeps = AdvantageKit.json, PathplannerLib.json, Phoenix6-frc2026-latest.json, REVLib.json, WPILibNewCommands.json, photonlib.json. 8793-2026-Robot = PathplannerLib-2026.1.2, Phoenix6-26.1.1, WPILibNewCommands, photonlib (no AdvantageKit, no REVLib). reefscape2025 = AdvantageKit, PathplannerLib-2025.2.7, Phoenix6-frc2025-latest, REVLib, WPILibNewCommands. So: Java, command-based v2, CTRE-primary with REV secondary, PathPlanner for autos, PhotonVision for vision, AdvantageKit sometimes.\n\n=== WPILIB 2026 (the season that just ended) ===\nKickoff release 2026.1.1 (2026-01-09); latest 2026.2.1 (adds 2026 field images + AprilTag layout); API docs published as 2026.2.2. Deliberately a light year: \"171 commits since 2025.3.2 by 51 contributors, touching over 1,100 files, 41,210 added / 32,403 removed\" and the team states there are \"less changes for 2026 than in previous years because the team is heavily focused on the 2027 Systemcore control system\" (wpilib.org/blog/2026-kickoff-release-of-wpilib). 2026 changes: units — added InchesPerSecondPerSecond, renamed AngularMomentumUnit.mult -> per, mph conversions, fixed units overload resolution; command-based — deprecated Command.schedule() in favor of CommandScheduler.getInstance().schedule(Command...), added Subsystem.idle(), CommandPtr marked [[nodiscard]]; Epilogue — superclass field & method logging, reflection access to non-public superclass fields, backend time/memory optimization, protobuf-serializable type logging; math — Eigen 5.0.0, structs for TrapezoidProfile.State and ExponentialProfile.State, Pose3d.nearest()/Translation3d.nearest(), 2D MathUtil.applyDeadband and MathUtil.copySignPow, new MathUtil.copyDirectionPow for joystick shaping; NT — control-message id range checks, Preferences.getNetworkTable(). Removals: Jaguar and other 2026-illegal motor controllers. Deprecated-for-2027: Shuffleboard, SmartDashboard, PathWeaver, RobotBuilder. Platforms: Win10/11 64-bit only, Ubuntu 22.04/24.04, macOS 13.3+. Project import from 2025 is MANDATORY (GradleRIO internals changed). AdvantageScope and Elastic ship in the WPILib installer (AdvantageScope bundled since 2024).\n\n=== WPILIB 2027 (the season being built for) === docs.wpilib.org/en/2027/\n\"The biggest control system update since the introduction of the cRIO.\" roboRIO -> SystemCore; every team gets a SystemCore in the 2027 KoP; no roboRIOs in 2027. Java 25, C++23, latest-Ubuntu-LTS only, Win10 support ended. Java packages edu.wpi.first -> org.wpilib; C++ frc:: -> wpi::; VS Code importer performs the conversion. Commands v3 (Java only, coroutine-based, includes a declarative state machine API) alongside retained Commands v2. OpMode framework (FTC-style, optional; TimedRobot still works). Telemetry/Tunable API replaces the SmartDashboard API. Field pose origin moves to field CENTER with scoring table at top (180 deg rotation from 2026). NT3 fully removed. Removed: Shuffleboard, SmartDashboard, PathWeaver, RobotBuilder, LabVIEW, Relay, AnalogOutput, AnalogGyro, SPI + all SPI IMUs, DMA, DigitalGlitchFilter, Counter, Ultrasonic, Interrupts, AnalogTrigger, Servo, AxisCamera, Nidec Dynamo, RamseteCommand/Controller, PIDCommand/ProfiledPIDCommand/TrapezoidProfileCommand, mutable Java units, robotInit(), MathUtil.clamp(), Rotation2d.value. Gamepad classes unified into one Gamepad class. SystemCore adds multiple CAN buses, Smart IO, onboard IMU, Expansion Hub over USB. GradleRIO on Gradle 9.4.1, ZGC default, UTF-8 enforced. Currently at Alpha 5/6; 50 alpha teams; testing repo github.com/wpilibsuite/SystemcoreTesting. 2027 kickoff Jan 9 2027, game BIOCORE.\n\n=== VENDOR VERSIONS (2026 season, verified) ===\nPhoenix 6 / Phoenix-Libs: 26.1.0 (kickoff 2026-01-07), 26.1.1, 26.1.2, 26.1.3, 26.2.0, 26.3.0 (2026-05-26 = latest 2026); 26.50.0-alpha-1 (2026-06-12) for WPILib 2027 alpha 5/6 on SystemCore. 2026 changes: (id, String canbus) constructors deprecated, removed 2027 — use CANBus object; C++20 minimum; Linux ARM32 dropped; Signal Logger gains protobuf + WPILib Structs + arbitrary user signals + an Epilogue Backend; OptimizeBusUtilization default 4 Hz (was 0); HootAutoReplay API; DifferentialMechanism reworked to match the swerve API; swerve LinearPath API + WheelForceCalculator + SeedFieldCentric() overload; Kraken X44 FOC + sim; Talon FXS generic sensored BLDC to 24V; MotionMagicAtTarget status signal; StatusSignal.IsNear(), StatusSignalCollection; C# parity via NuGet. Tuner X 2026.2.0.0: Elevator Generator, multi-device control/plot, hoot Log Analysis beta, CANdle animation preview, swerve generator support for brushed Talon FXS + remote CANdi/CANcoder. SystemCore status: CANBus.systemCore(int busId) works; Phoenix 5 unavailable; no offline installer; Tuner cannot deploy a temporary diagnostic server.\nREVLib: 2026.0.0 through 2026.0.5 (latest; warns on non-2026 SPARK firmware). New: MAXSpline Encoder via DetachedEncoder; StatusLogger (official logging, adapted from URCL, writes .revlog readable by AdvantageScope); feedforward params kV/kA/kS/kG/kCos/kCosRatio; closed-loop status signals isAtSetpoint/setpoint/selectedClosedLoopSlot; per-signal status periods (no more manual frame math); motor/encoder config presets. Deprecations: setReference() -> setSetpoint(); configure() deprecated in 2026; SmartMotion removed (use MAXMotion). Breaking: automatic fault clearing removed from SparkMax/SparkFlex/ServoHub init.\nPathPlanner: v2026.1.1 (2026-01-11), v2026.1.2 (2026-01-12, field image scale fix) = latest. Author mjansen: \"There have been no major changes of note this year besides the addition of the 2026 field image. My plan is to hold off on major changes for the 2027 switch to SystemCore.\" The trajectory-generation rework (torque-based accel limits, traction control) landed in v2025.1.1, not 2026. vendordep https://3015rangerrobotics.github.io/pathplannerlib/PathplannerLib.json\nChoreo: v2026.0.0 (2026-01-11), v2026.0.1, v2026.0.2 (2026-03-02, Java codegen + alliance-flipped view), v2026.0.3 (2026-04-06, rotateAround/mirrorX/mirrorY, warmup command, angular-accel limits in the swerve solver) = latest. Breaking: DifferentialSample gains alpha (regenerate all trajectories); AutoRoutine.anyDone() removed; AutoTrajectory.done(int) removed; C++ kTrajSpecVersion -> kTrajSchemaVersion. vendordep https://choreo.autos/lib/ChoreoLib2026.json\nPhotonVision: v2026.1.1, 2026.2.1, 2026.2.2, 2026.3.1, 2026.3.2, 2026.3.3, 2026.3.4 (latest 2026); v2027.0.0-alpha-2 exists. 2026: Java 25, Jackson -> Avaje Jsonb, Vue 3 frontend, 2026 FUEL gamepiece detection models, up to 127 targets, rotated-rect RKNN detections, Rubik Pi 3 and Limelight 4 support, min 100 calibration images, PNP_DISTANCE_TRIG_SOLVE in Python, deprecated pose-estimator methods removed, resetHeadingData(Rotation3d).\nLimelight: LimelightHelpers (single-file drop-in, not a vendordep) with MegaTag2 — getBotPoseEstimate_wpiBlue_MegaTag2()/_wpiRed_MegaTag2(), SetRobotOrientation(yaw, yawRate, pitch, pitchRate, roll, rollRate). 2026 adds Point-of-Interest map offsets (\"Use Map POI Offsets\") to aim at Hub center regardless of targeted tag. YALL (Yet Another Limelight Library) is an improved fork under YASS.\nAdvantageKit: v26.0.0 (2026-01-08), v26.0.1 (2026-02-25), v26.0.2 (2026-03-19, WPILib 2026.2.1, unit-metadata replay fixes) = latest; v27.0.0-alpha-4 restricted to SystemCore alpha teams. 2026: unit metadata on all logging interfaces (unit-aware AdvantageScope graphing), NT client logging in SystemStats, exception capture in console logging, generate3dMechanism, Color logging, TalonFX(S) swerve template. 600+ teams in 2025, 200+ on the swerve templates. License: BSD-3-Clause-style, \"Copyright (c) 2021-2026 Littleton Robotics. All rights reserved.\" + non-endorsement clause.\nAdvantageScope: v26.0.0 (2026-01-08), v26.0.1, v26.0.2 (2026-04-25). 2026: unit-aware graphing with implicit conversion, experimental FTC support (fields, coordinate systems, Road Runner logs, FTC Dashboard live), .revlog support, CSV import, roboRIO log download 2-4x faster (~80 Mb/s vs 25). Reads WPILOG, DS logs, Hoot (CTRE), RLOG, .revlog. Bundled with WPILib. URCL remains available in 2026 alongside REV's official StatusLogger.\nElastic: 2026.0.0 (2026-01-09), 2026.1.0, 2026.1.1, 2026.1.2 (2026-03-02) = latest. 2026: web-browser build, individual struct-field display/drag-out, field widget zoom/pan, command-widget fill option, year-scoped update checks.\nYAGSL / YASS: 2026.1.13 → 2026.8.05 (2026-08-05, notes include \"Update to 2027\"). YASS = YAGSL (swerve, JSON config, vendor-agnostic across REV/CTRE/Studica/ThriftyNova, vision fusion, sim) + YAMS (mechanisms, SmartMotorController, live tuning, LGPLv3) + YALL (Limelight) + YAMG (yamgen.com web mechanism generator). 537 FRC teams used YASS libraries this season. Founding teams 3481, 457, 3561, 9658.\nmaple-sim: v0.4.0-beta (2026 Rebuilt, WPILib 2026.2.1); prior stable v0.3.14. Explicitly beta.\nElastic/others: Studica NavX https://dev.studica.com/maven/release/2026/json/Studica-2026.0.0.json ; ReduxLib 2026.1.2 https://frcsdk.reduxrobotics.com/ReduxLib_2026.json ; DogLog https://doglog.dev/vendordep.json (Team 581); 3061-lib v2026.1.0; FrcCatalyst v1.8.0 (MIT, WPILib 2026.2.1 + Phoenix 6 26.1.1, Java 17+).\n2026 game: REBUILT presented by Haas, kickoff 2026-01-10, gamepiece FUEL (5.91 in foam ball), Tower climb; 3,791 teams, 56 regionals, 144 district events.",
  "summary": "The 2026 FRC season was deliberately a caretaker year for WPILib (171 commits since 2025.3.2 by 51 contributors) because the team redirected effort at the 2027 SystemCore control system, and the same freeze propagated through the ecosystem — PathPlanner's author publicly stated \"no major changes of note this year besides the addition of the 2026 field image. My plan is to hold off on major changes for the 2027 switch to SystemCore.\" That makes the design question for PumpkinLib not \"what's missing in 2026\" but \"which 2027 break do we absorb for teams\": org.wpilib package renames, Java 25/C++23, Commands v3 alongside v2, a Telemetry/Tunable API replacing SmartDashboard, NT3 and Shuffleboard/SmartDashboard/PathWeaver/RobotBuilder deleted, mutable units deleted, gamepad classes unified, and the field pose origin relocating to field center with a 180-degree rotation. The mature-and-free layer is now large and should be depended on wholesale — AdvantageKit v26.0.2 for replay, AdvantageScope v26.0.2 and Elastic 2026.1.2 for visualization, PathPlannerLib v2026.1.2 and Choreo v2026.0.3 for autos, PhotonVision v2026.3.4 and MegaTag2 for vision, Phoenix 6 26.3.0 and REVLib 2026.0.5 for motors, YAGSL for swerve, WPILib Epilogue/Alert/SysIdRoutine for telemetry, faults, and characterization. Two independent 2026 projects (YAMS under YASS, used with YAGSL by 537 teams; FrcCatalyst v1.8.0, MIT, CTRE-only) converged on the same answer for small teams — declarative mechanism config builders that derive control, limits, simulation, telemetry, and SysId from one declaration — which validates the demand but also means PumpkinLib cannot differentiate on the builder alone. The genuinely unclaimed ground, evidenced directly in Chief Delphi threads, is: a standard Tunable primitive (six incompatible community implementations, none in WPILib for 2026, the 2027 API still an unmerged PR), pre-authored fault Alerts and an executable swerve bring-up/verification wizard (the documented #1 small-team failure is configuration — CAN IDs, offsets, inversions — not math), pre-built @CustomLoggerFor loggers for every common vendor device, and a telemetry-budget layer that stops small teams from responding to loop overruns by logging less. The hardest external constraint is kickoff-week vendor lag: AdvantageKit's 2026 swerve templates shipped weeks late waiting on PathPlanner and Studica, so PumpkinLib's core must have zero hard vendordep dependencies with vendor adapters as separate optional artifacts."
}
